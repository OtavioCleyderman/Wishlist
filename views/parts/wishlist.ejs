<ol class="breadcrumb" >
	<a href="/" class="breadcrumb-item">Home > </a> <li class="breadcrumb-item-wishlist"> Lista de desejos</li>
</ol>

<section id="cards-wishlist" class="cards"></section>


<script>
	
		let wishlistData = JSON.parse(localStorage.getItem('wishlist'))
		const sectionCard = document.getElementById('cards-wishlist')

		// Verifica se a wishlist está vazia
		if( wishlistData.length == 0){
			// Se estiver vazia, renderiza uma mensagem h1 dentro de uma div informando que a lista está vazia
			const divText = document.createElement('div')
			divText.className = 'no-wishlist'
			const h1Text = document.createElement('h1')
			h1Text.className = 'no-wishlist-title'
			h1Text.innerText = `Sem itens em sua lista de desejos no momento! 😔`
			divText.appendChild(h1Text)
			sectionCard.appendChild(divText)

		} else {
			// Se não estiver vazia redendiza um card com as informações do produto de desejo do cliente
			for(let i = 0; i < wishlistData.length; i++) {

				const svgSrc = '/icons/x-circle.svg'
	
				const card = document.createElement('div')
				card.className = 'card'
	
				const cardImage = document.createElement('img')
				cardImage.className = 'card-image'
				cardImage.src = wishlistData[i].imageSrc
	
				const cardTitle = document.createElement('div')
				cardTitle.className = 'card-title'
				cardTitle.innerText = wishlistData[i].title
	
				const cardPrice = document.createElement('div')
				cardPrice.className = 'card-price'
				cardPrice.innerText = wishlistData[i].price
	
				const cardButton = document.createElement('button')
				cardButton.className = 'card-wishlist-delete'
				
				const imageSvg = document.createElement('img')
				imageSvg.className = 'iconDeleteWishlist'
				imageSvg.src = svgSrc
	
				card.append(cardImage)
				card.append(cardTitle)
				card.append(cardPrice)
				cardButton.appendChild(imageSvg)
				card.append(cardButton)

				sectionCard.appendChild(card)
		}

	 } 

</script>




